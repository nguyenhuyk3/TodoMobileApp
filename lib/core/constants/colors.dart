// ignore_for_file: non_constant_identifier_names

import 'package:flutter/material.dart';

class AppColors {
  // Input border
  final UNFOCUSED_BORDER_IP_COLOR = const Color.fromARGB(255, 152, 150, 150);
  final FOCUSED_BORDER_IP_COLOR = Color.fromARGB(6, 0, 0, 0);
  final FOCUSED_ERROR_BORDER_IP_COLOR = Colors.redAccent;
  final UNFOCUSED_ERROR_BORDER_IP_COLOR = Color.fromRGBO(255, 212, 59, 1);
  
  // Background
  final PRIMARY_BG_COLOR = Colors.white;
  final INPUT_BG_COLOR = Color.fromARGB(255, 247, 247, 247);
  final BG_ICON_COLOR = Color.fromARGB(255, 243, 243, 243);

  // Label
  final FOCUSED_LABEL_COLOR = Colors.black;
  final LABEL_COLOR = Colors.black;
  final ERROR_LABEL = Colors.redAccent;

  final ERROR_COLOR = Colors.redAccent;

  // Button
  final PRIMARY_BUTTON_COLOR = Color.fromRGBO(253, 205, 32, 1);

  // Title
  final HEADER_PAGE_COLOR = Colors.black;

  // Text
  final PRIMARY_TEXT_COLOR = Color(0xFF2C2C2C);

  //
  final PRIMARY_APP_COLOR = Color.fromRGBO(249, 202, 33, 1);
}
